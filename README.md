# Soundwave

A command-line utility written in C for analyzing and modifying uncompressed PCM `.wav` audio files. 
The program processes audio data by reading from standard input (`stdin`) and writing the results to standard output (`stdout`).

## Compilation

Compile the source code using standard C compilers like `gcc`:

```bash
gcc -o soundwave soundwave.c -lm
```
